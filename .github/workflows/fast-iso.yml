name: Build Fast ISO

on:
  push:
    branches: [ "2.12" ]
  pull_request:
    branches: [ "2.12" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      #      run: make fast-iso
    run: echo LAST TAG is ${{ github.ref_name }}
