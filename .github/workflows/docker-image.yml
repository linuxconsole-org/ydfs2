name: Docker Image CI

on:
  push:
    branches: [ "2.12" ]
  pull_request:
    branches: [ "2.12" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build ./2.12 --file 2.12/Dockerfile --tag ydfs-2.12:$(date +%s)
